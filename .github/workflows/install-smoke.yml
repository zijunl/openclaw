name: Install Smoke

# Fork note: scheduled runs disabled on this fork — these maintenance automations
# require upstream-only secrets (GitHub App private keys, LLM API keys,
# registry credentials) that are not available on forks. Manual dispatch still works.
on:
  workflow_dispatch:
    inputs:
      run_bun_global_install_smoke:
        description: Run the Bun global install and runtime smoke
        required: false
        default: false
        type: boolean
      update_baseline_version:
        description: Baseline openclaw version or dist-tag for installer update smoke
        required: false
        default: latest
        type: string

permissions:
  actions: read
  contents: read
  packages: read

concurrency:
  group: ${{ github.event_name == 'workflow_dispatch' && format('{0}-{1}', github.workflow, github.run_id) || format('{0}-{1}', github.workflow, github.ref) }}
  cancel-in-progress: ${{ github.event_name != 'workflow_dispatch' }}

jobs:
  install_smoke:
    permissions:
      actions: read
      contents: read
      packages: read
    uses: ./.github/workflows/install-smoke-reusable.yml
    with:
      allow_unreleased_changelog: true
      ref: ${{ github.sha }}
      run_bun_global_install_smoke: ${{ github.event_name == 'schedule' || inputs.run_bun_global_install_smoke }}
      update_baseline_version: ${{ inputs.update_baseline_version || 'latest' }}
